import { build } from "@aromix/core";

export default build({
  entry:     "src/index.ts",
  outDir:    "dist",
  platform:  "{{platform}}",
  tsConfig:  "tsconfig.json",
  sourcemap: false,
  minify:    false,
});
